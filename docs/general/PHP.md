## Loose comparison (==)

string == integer -> PHP takes first chaarcter of string and convert to integer, if not by default is 0

5 == "5xyz" evaluates to true
0 == "Example string" evaluates to true